<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Derrick Peralta — Funnel Builder & Automation Specialist</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
<style>
  /* ── RESET & BASE ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --bg: #080c10;
    --surface: #0f1419;
    --surface2: #161d25;
    --border: rgba(255,255,255,0.07);
    --gold: #c9a84c;
    --gold-dim: rgba(201,168,76,0.15);
    --gold-glow: rgba(201,168,76,0.3);
    --text: #e8eaed;
    --muted: #7a8494;
    --accent: #3b82f6;
    --font-head: 'Syne', sans-serif;
    --font-body: 'DM Sans', sans-serif;
    --radius: 12px;
    --transition: 0.25s cubic-bezier(0.4,0,0.2,1);
  }
  html { scroll-behavior: smooth; }
  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.6;
    overflow-x: hidden;
  }
  a { color: inherit; text-decoration: none; }

  /* ── NOISE OVERLAY ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 0;
    opacity: 0.4;
  }

  /* ── NAV ── */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 5%;
    background: rgba(8,12,16,0.85);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid var(--border);
    transition: box-shadow var(--transition);
  }
  nav.scrolled { box-shadow: 0 2px 40px rgba(0,0,0,0.5); }
  .nav-logo {
    font-family: var(--font-head);
    font-weight: 800;
    font-size: 1.1rem;
    color: var(--gold);
    letter-spacing: 0.04em;
  }
  .nav-links { display: flex; gap: 32px; }
  .nav-links a {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--muted);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    transition: color var(--transition);
  }
  .nav-links a:hover { color: var(--gold); }
  .nav-cta {
    background: var(--gold);
    color: #080c10 !important;
    padding: 9px 20px;
    border-radius: 6px;
    font-weight: 700 !important;
    font-size: 0.8rem !important;
    transition: opacity var(--transition) !important;
  }
  .nav-cta:hover { opacity: 0.85 !important; color: #080c10 !important; }

  /* ── SECTION BASE ── */
  section { position: relative; z-index: 1; }
  .container { max-width: 1100px; margin: 0 auto; padding: 0 5%; }
  .section-label {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 14px;
  }
  .section-title {
    font-family: var(--font-head);
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 800;
    line-height: 1.15;
    margin-bottom: 16px;
  }
  .section-sub {
    color: var(--muted);
    font-size: 1rem;
    max-width: 560px;
    line-height: 1.7;
    margin-bottom: 56px;
  }

  /* ── HERO ── */
  #hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 120px 5% 80px;
    position: relative;
    overflow: hidden;
  }
  .hero-glow {
    position: absolute;
    width: 600px; height: 600px;
    background: radial-gradient(circle, rgba(201,168,76,0.12) 0%, transparent 70%);
    top: -100px; right: -100px;
    pointer-events: none;
    animation: pulse 6s ease-in-out infinite;
  }
  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 0.7; }
    50% { transform: scale(1.1); opacity: 1; }
  }
  .hero-inner { max-width: 1100px; margin: 0 auto; width: 100%; }
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--gold-dim);
    border: 1px solid var(--gold-glow);
    border-radius: 100px;
    padding: 6px 16px;
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--gold);
    letter-spacing: 0.08em;
    margin-bottom: 28px;
    opacity: 0;
    animation: fadeUp 0.6s 0.2s forwards;
  }
  .hero-badge::before { content: '●'; font-size: 0.5rem; animation: blink 1.5s infinite; }
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.3} }
  h1 {
    font-family: var(--font-head);
    font-size: clamp(2.6rem, 6vw, 4.2rem);
    font-weight: 800;
    line-height: 1.08;
    letter-spacing: -0.02em;
    margin-bottom: 24px;
    opacity: 0;
    animation: fadeUp 0.7s 0.35s forwards;
  }
  h1 span { color: var(--gold); }
  .hero-sub {
    font-size: clamp(1rem, 1.8vw, 1.15rem);
    color: var(--muted);
    max-width: 540px;
    line-height: 1.75;
    margin-bottom: 40px;
    opacity: 0;
    animation: fadeUp 0.7s 0.5s forwards;
  }
  .hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    opacity: 0;
    animation: fadeUp 0.7s 0.65s forwards;
  }
  .btn-primary {
    background: var(--gold);
    color: #080c10;
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 0.9rem;
    padding: 14px 28px;
    border-radius: 8px;
    border: none;
    cursor: pointer;
    transition: transform var(--transition), box-shadow var(--transition);
    display: inline-block;
  }
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px var(--gold-glow);
  }
  .btn-outline {
    background: transparent;
    color: var(--text);
    font-family: var(--font-body);
    font-weight: 500;
    font-size: 0.9rem;
    padding: 14px 28px;
    border-radius: 8px;
    border: 1px solid var(--border);
    cursor: pointer;
    transition: border-color var(--transition), color var(--transition);
    display: inline-block;
  }
  .btn-outline:hover { border-color: var(--gold); color: var(--gold); }

  /* ── STATS BAR ── */
  .stats-bar {
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 36px 5%;
  }
  .stats-inner {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
  }
  .stat-item {
    text-align: center;
    padding: 0 20px;
    border-right: 1px solid var(--border);
  }
  .stat-item:last-child { border-right: none; }
  .stat-num {
    font-family: var(--font-head);
    font-size: 2.2rem;
    font-weight: 800;
    color: var(--gold);
    display: block;
  }
  .stat-label { font-size: 0.82rem; color: var(--muted); margin-top: 4px; }

  /* ── WHAT I DO ── */
  #services { padding: 100px 0; }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  .service-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 32px;
    transition: border-color var(--transition), transform var(--transition), box-shadow var(--transition);
    position: relative;
    overflow: hidden;
  }
  .service-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    opacity: 0;
    transition: opacity var(--transition);
  }
  .service-card:hover {
    border-color: var(--gold-glow);
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.3);
  }
  .service-card:hover::before { opacity: 1; }
  .service-icon { font-size: 2rem; margin-bottom: 16px; }
  .service-card h3 {
    font-family: var(--font-head);
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 10px;
  }
  .service-card p { font-size: 0.9rem; color: var(--muted); line-height: 1.65; }

  /* ── AUTOMATION SYSTEMS ── */
  #automation { padding: 100px 0; background: var(--surface); }
  .automation-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  .auto-card {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    transition: border-color var(--transition), transform var(--transition);
  }
  .auto-card:hover {
    border-color: var(--gold-glow);
    transform: translateY(-3px);
  }
  .auto-icon { font-size: 1.6rem; margin-bottom: 14px; display: block; }
  .auto-card h3 {
    font-family: var(--font-head);
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 8px;
  }
  .auto-card p { font-size: 0.87rem; color: var(--muted); line-height: 1.6; }

  /* ── WORKFLOW FLOW ── */
  #workflow { padding: 100px 0; }
  .flow-track {
    display: flex;
    align-items: stretch;
    gap: 0;
    overflow-x: auto;
    padding-bottom: 8px;
  }
  .flow-step {
    flex: 1;
    min-width: 130px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px 20px;
    text-align: center;
    position: relative;
    transition: border-color var(--transition), background var(--transition);
  }
  .flow-step:hover {
    border-color: var(--gold-glow);
    background: var(--surface2);
  }
  .flow-step:not(:last-child)::after {
    content: '→';
    position: absolute;
    right: -18px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--gold);
    font-size: 1.1rem;
    z-index: 2;
  }
  .flow-step + .flow-step { margin-left: 18px; }
  .flow-icon { font-size: 1.8rem; margin-bottom: 10px; }
  .flow-step h4 {
    font-family: var(--font-head);
    font-size: 0.82rem;
    font-weight: 700;
    margin-bottom: 6px;
    color: var(--gold);
  }
  .flow-step p { font-size: 0.78rem; color: var(--muted); line-height: 1.5; }

  /* ── WORKFLOW AUTOMATION (NEW SECTION) ── */
  #workflow-automation { padding: 100px 0; background: var(--surface); }
  .wa-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
  }
  .wa-left h2 { margin-bottom: 20px; }
  .wa-steps { display: flex; flex-direction: column; gap: 20px; margin-top: 36px; }
  .wa-step {
    display: flex;
    gap: 18px;
    align-items: flex-start;
    padding: 20px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    transition: border-color var(--transition);
  }
  .wa-step:hover { border-color: var(--gold-glow); }
  .wa-num {
    flex-shrink: 0;
    width: 36px; height: 36px;
    background: var(--gold-dim);
    border: 1px solid var(--gold-glow);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-head);
    font-size: 0.85rem;
    font-weight: 800;
    color: var(--gold);
  }
  .wa-step-text h4 {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: 0.95rem;
    margin-bottom: 4px;
  }
  .wa-step-text p { font-size: 0.85rem; color: var(--muted); }
  .wa-right {
    position: sticky;
    top: 100px;
  }
  .wa-diagram {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 32px;
  }
  .wa-diagram h3 {
    font-family: var(--font-head);
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 24px;
    color: var(--gold);
  }
  .pipeline-vis { display: flex; flex-direction: column; gap: 12px; }
  .pipeline-row {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .pipeline-dot {
    width: 10px; height: 10px;
    border-radius: 50%;
    background: var(--gold);
    flex-shrink: 0;
    box-shadow: 0 0 8px var(--gold-glow);
  }
  .pipeline-label { font-size: 0.82rem; color: var(--muted); flex: 1; }
  .pipeline-bar-wrap {
    flex: 2;
    background: var(--surface2);
    border-radius: 4px;
    height: 6px;
    overflow: hidden;
  }
  .pipeline-bar {
    height: 100%;
    background: linear-gradient(90deg, var(--gold), rgba(201,168,76,0.4));
    border-radius: 4px;
    animation: barGrow 1.5s ease forwards;
    transform-origin: left;
  }
  @keyframes barGrow { from { width: 0; } }
  .tools-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 24px;
  }
  .tool-tag {
    background: var(--gold-dim);
    border: 1px solid var(--gold-glow);
    color: var(--gold);
    font-size: 0.78rem;
    font-weight: 600;
    padding: 6px 14px;
    border-radius: 100px;
    letter-spacing: 0.04em;
  }

  /* ── FUNNELS ── */
  #funnels { padding: 100px 0; }
  .funnels-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  .funnel-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    transition: border-color var(--transition), transform var(--transition), box-shadow var(--transition);
    display: flex;
    flex-direction: column;
    gap: 12px;
    cursor: pointer;
  }
  .funnel-card:hover {
    border-color: var(--gold-glow);
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.35);
  }
  .funnel-emoji { font-size: 2rem; }
  .funnel-card h3 {
    font-family: var(--font-head);
    font-size: 1rem;
    font-weight: 700;
    line-height: 1.3;
  }
  .funnel-card p { font-size: 0.85rem; color: var(--muted); flex: 1; }
  .funnel-link {
    font-size: 0.8rem;
    color: var(--gold);
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 4px;
    transition: gap var(--transition);
  }
  .funnel-card:hover .funnel-link { gap: 10px; }

  /* ── RESUME ── */
  #resume { padding: 100px 0; background: var(--surface); }
  .resume-grid {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 48px;
    align-items: start;
  }
  .resume-skills h3 {
    font-family: var(--font-head);
    font-size: 1rem;
    font-weight: 700;
    color: var(--gold);
    margin-bottom: 16px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.78rem;
  }
  .skill-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-bottom: 36px;
  }
  .skill-pill {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 7px 14px;
    font-size: 0.82rem;
    color: var(--text);
    transition: border-color var(--transition), color var(--transition);
  }
  .skill-pill:hover { border-color: var(--gold-glow); color: var(--gold); }
  .edu-card {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 24px;
    margin-top: 0;
  }
  .edu-card h4 {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: 0.95rem;
    margin-bottom: 4px;
  }
  .edu-card span { font-size: 0.82rem; color: var(--muted); }
  .resume-exp { display: flex; flex-direction: column; gap: 20px; }
  .exp-item {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 24px;
    position: relative;
    transition: border-color var(--transition);
  }
  .exp-item:hover { border-color: var(--gold-glow); }
  .exp-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; margin-bottom: 8px; }
  .exp-title {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: 0.97rem;
  }
  .exp-date {
    font-size: 0.75rem;
    color: var(--gold);
    background: var(--gold-dim);
    border: 1px solid var(--gold-glow);
    padding: 3px 10px;
    border-radius: 100px;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .exp-company { font-size: 0.85rem; color: var(--muted); margin-bottom: 12px; }
  .exp-bullets { list-style: none; display: flex; flex-direction: column; gap: 6px; }
  .exp-bullets li {
    font-size: 0.85rem;
    color: var(--muted);
    padding-left: 16px;
    position: relative;
    line-height: 1.55;
  }
  .exp-bullets li::before {
    content: '▸';
    position: absolute;
    left: 0;
    color: var(--gold);
    font-size: 0.7rem;
    top: 2px;
  }

  /* ── TOOLS ── */
  #tools { padding: 100px 0; }
  .tools-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  .tool-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    display: flex;
    align-items: flex-start;
    gap: 18px;
    transition: border-color var(--transition), transform var(--transition);
  }
  .tool-card:hover {
    border-color: var(--gold-glow);
    transform: translateY(-3px);
  }
  .tool-em { font-size: 2rem; flex-shrink: 0; }
  .tool-card h3 {
    font-family: var(--font-head);
    font-size: 0.97rem;
    font-weight: 700;
    margin-bottom: 6px;
  }
  .tool-card p { font-size: 0.84rem; color: var(--muted); line-height: 1.55; }

  /* ── CTA ── */
  #cta {
    padding: 120px 5%;
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  .cta-glow {
    position: absolute;
    width: 500px; height: 500px;
    background: radial-gradient(circle, rgba(201,168,76,0.1) 0%, transparent 70%);
    top: 50%; left: 50%;
    transform: translate(-50%,-50%);
    pointer-events: none;
  }
  .cta-inner { position: relative; z-index: 1; max-width: 600px; margin: 0 auto; }
  .cta-inner h2 {
    font-family: var(--font-head);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    margin-bottom: 20px;
    line-height: 1.15;
  }
  .cta-inner h2 span { color: var(--gold); }
  .cta-inner p { color: var(--muted); margin-bottom: 40px; line-height: 1.7; }

  /* ── FOOTER ── */
  footer {
    border-top: 1px solid var(--border);
    padding: 36px 5%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.82rem;
    color: var(--muted);
  }
  .footer-logo {
    font-family: var(--font-head);
    font-weight: 800;
    color: var(--gold);
    font-size: 1rem;
  }

  /* ── SCROLL ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(22px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.55s ease, transform 0.55s ease;
  }
  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }
  .reveal-delay-1 { transition-delay: 0.08s; }
  .reveal-delay-2 { transition-delay: 0.16s; }
  .reveal-delay-3 { transition-delay: 0.24s; }
  .reveal-delay-4 { transition-delay: 0.32s; }
  .reveal-delay-5 { transition-delay: 0.40s; }

  /* ── RESPONSIVE ── */
  @media (max-width: 900px) {
    .services-grid, .automation-grid, .funnels-grid, .tools-grid { grid-template-columns: 1fr 1fr; }
    .stats-inner { grid-template-columns: repeat(2, 1fr); }
    .stat-item:nth-child(2) { border-right: none; }
    .stat-item:nth-child(3) { border-right: 1px solid var(--border); border-top: 1px solid var(--border); }
    .stat-item:nth-child(4) { border-top: 1px solid var(--border); border-right: none; }
    .wa-grid, .resume-grid { grid-template-columns: 1fr; }
    .wa-right { position: static; }
    .nav-links { display: none; }
  }
  @media (max-width: 600px) {
    .services-grid, .automation-grid, .funnels-grid, .tools-grid { grid-template-columns: 1fr; }
    .stats-inner { grid-template-columns: repeat(2, 1fr); }
    .flow-track { flex-direction: column; }
    .flow-step:not(:last-child)::after { content: '↓'; right: auto; bottom: -18px; top: auto; left: 50%; transform: translateX(-50%); }
    .flow-step + .flow-step { margin-left: 0; margin-top: 18px; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav id="navbar">
  <div class="nav-logo">DP</div>
  <div class="nav-links">
    <a href="#services">Services</a>
    <a href="#funnels">Funnels</a>
    <a href="#workflow-automation">Automation</a>
    <a href="#resume">Resume</a>
    <a href="#cta" class="nav-cta">Book a Call</a>
  </div>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-glow"></div>
  <div class="hero-inner">
    <div class="hero-badge">Available for Projects</div>
    <h1>I'm Derrick Peralta —<br /><span>Funnel Builder &<br />Automation Specialist</span></h1>
    <p class="hero-sub">I help agencies and businesses build high-converting funnels and automated marketing systems using GoHighLevel, Zapier, and AI-powered workflows.</p>
    <div class="hero-actions">
      <a href="#funnels" class="btn-primary">View My Work</a>
      <a href="#cta" class="btn-outline">Book a Free Call</a>
    </div>
  </div>
</section>

<!-- STATS -->
<div class="stats-bar">
  <div class="stats-inner">
    <div class="stat-item reveal">
      <span class="stat-num">6+</span>
      <div class="stat-label">Funnels Built</div>
    </div>
    <div class="stat-item reveal reveal-delay-1">
      <span class="stat-num">100%</span>
      <div class="stat-label">No-Code Delivery</div>
    </div>
    <div class="stat-item reveal reveal-delay-2">
      <span class="stat-num">24/7</span>
      <div class="stat-label">Automated Systems</div>
    </div>
    <div class="stat-item reveal reveal-delay-3">
      <span class="stat-num">5+</span>
      <div class="stat-label">Industries Served</div>
    </div>
  </div>
</div>

<!-- SERVICES -->
<section id="services">
  <div class="container">
    <div class="section-label reveal">What I Do</div>
    <h2 class="section-title reveal">End-to-End Funnel &<br />Automation Systems</h2>
    <p class="section-sub reveal">I build conversion-focused funnels and automated systems using GoHighLevel and modern no-code tools.</p>
    <div class="services-grid">
      <div class="service-card reveal">
        <div class="service-icon">⚡</div>
        <h3>Funnel Design</h3>
        <p>High-converting landing pages and sales funnels designed to capture leads and drive conversions across industries.</p>
      </div>
      <div class="service-card reveal reveal-delay-1">
        <div class="service-icon">🔄</div>
        <h3>Marketing Automation</h3>
        <p>Automated follow-ups using email, SMS, and AI workflows to nurture leads and increase conversions on autopilot.</p>
      </div>
      <div class="service-card reveal reveal-delay-2">
        <div class="service-icon">📊</div>
        <h3>CRM Systems</h3>
        <p>Pipeline setups, lead tracking, and automated systems to manage and convert leads efficiently at scale.</p>
      </div>
    </div>
  </div>
</section>

<!-- AUTOMATION SYSTEMS -->
<section id="automation">
  <div class="container">
    <div class="section-label reveal">Automation Systems</div>
    <h2 class="section-title reveal">Built to Run Your<br />Business 24/7</h2>
    <p class="section-sub reveal">From lead capture to appointment booking, every step is automated so no opportunity slips through.</p>
    <div class="automation-grid">
      <div class="auto-card reveal">
        <span class="auto-icon">⚙️</span>
        <h3>CRM Pipeline Setup</h3>
        <p>Deal stages, opportunity tracking, and automated lead movement through the sales pipeline.</p>
      </div>
      <div class="auto-card reveal reveal-delay-1">
        <span class="auto-icon">📩</span>
        <h3>Automated Follow-Up Sequences</h3>
        <p>Email and SMS automation workflows designed to nurture leads and increase conversions.</p>
      </div>
      <div class="auto-card reveal reveal-delay-2">
        <span class="auto-icon">🎯</span>
        <h3>Lead Capture & Routing</h3>
        <p>Automatically capture leads from forms, chat, or landing pages and route them into the CRM pipeline.</p>
      </div>
      <div class="auto-card reveal reveal-delay-3">
        <span class="auto-icon">📅</span>
        <h3>Appointment Booking Automation</h3>
        <p>Calendar systems that allow prospects to schedule appointments instantly with automated reminders.</p>
      </div>
      <div class="auto-card reveal reveal-delay-4">
        <span class="auto-icon">📊</span>
        <h3>Sales & Lead Tracking</h3>
        <p>Track opportunities, conversations, and sales performance using CRM pipelines and dashboards.</p>
      </div>
      <div class="auto-card reveal reveal-delay-5">
        <span class="auto-icon">🔗</span>
        <h3>Integration & Workflow Automation</h3>
        <p>Connect tools and automate processes across platforms using advanced workflows and integrations.</p>
      </div>
    </div>
  </div>
</section>

<!-- HOW IT WORKS FLOW -->
<section id="workflow">
  <div class="container">
    <div class="section-label reveal">How It Works</div>
    <h2 class="section-title reveal">From Lead to<br />Closed Deal — Automated</h2>
    <p class="section-sub reveal">Watch how leads move through a fully automated system, no manual effort required.</p>
    <div class="flow-track">
      <div class="flow-step reveal">
        <div class="flow-icon">🎯</div>
        <h4>Lead Captured</h4>
        <p>Visitors submit via landing pages</p>
      </div>
      <div class="flow-step reveal reveal-delay-1">
        <div class="flow-icon">⚙️</div>
        <h4>CRM Pipeline</h4>
        <p>Lead stored & tracked automatically</p>
      </div>
      <div class="flow-step reveal reveal-delay-2">
        <div class="flow-icon">📩</div>
        <h4>Follow-Ups</h4>
        <p>Automated emails nurture leads</p>
      </div>
      <div class="flow-step reveal reveal-delay-3">
        <div class="flow-icon">📅</div>
        <h4>Booking</h4>
        <p>Leads self-schedule calls instantly</p>
      </div>
      <div class="flow-step reveal reveal-delay-4">
        <div class="flow-icon">💰</div>
        <h4>Closed Deal</h4>
        <p>Leads convert into paying clients</p>
      </div>
    </div>
  </div>
</section>

<!-- WORKFLOW AUTOMATION (NEW SECTION) -->
<section id="workflow-automation">
  <div class="container">
    <div class="wa-grid">
      <div class="wa-left">
        <div class="section-label reveal">Workflow Automation</div>
        <h2 class="section-title reveal">Intelligent Workflows<br />That Run Behind the Scenes</h2>
        <p class="section-sub reveal" style="margin-bottom: 0;">I design multi-step automation workflows that connect your tools, route your leads, and deliver the right message at the right time — without lifting a finger.</p>
        <div class="wa-steps">
          <div class="wa-step reveal">
            <div class="wa-num">01</div>
            <div class="wa-step-text">
              <h4>Trigger-Based Entry</h4>
              <p>Workflows launch when a form is submitted, a tag is applied, or a pipeline stage changes.</p>
            </div>
          </div>
          <div class="wa-step reveal reveal-delay-1">
            <div class="wa-num">02</div>
            <div class="wa-step-text">
              <h4>Smart Conditions & Branching</h4>
              <p>If/else logic routes leads down different paths based on their actions or data.</p>
            </div>
          </div>
          <div class="wa-step reveal reveal-delay-2">
            <div class="wa-num">03</div>
            <div class="wa-step-text">
              <h4>Multi-Channel Outreach</h4>
              <p>Automated emails, SMS, and internal team notifications fire in perfect sequence.</p>
            </div>
          </div>
          <div class="wa-step reveal reveal-delay-3">
            <div class="wa-num">04</div>
            <div class="wa-step-text">
              <h4>Cross-Platform Integrations</h4>
              <p>Connect GHL, Zapier, n8n, and Make to sync data across your entire tech stack.</p>
            </div>
          </div>
          <div class="wa-step reveal reveal-delay-4">
            <div class="wa-num">05</div>
            <div class="wa-step-text">
              <h4>AI-Powered Logic</h4>
              <p>Integrate GPT-powered responses and AI lead qualification into your workflows.</p>
            </div>
          </div>
        </div>
      </div>
      <div class="wa-right">
        <div class="wa-diagram reveal">
          <h3>⚙️ Live Pipeline Snapshot</h3>
          <div class="pipeline-vis">
            <div class="pipeline-row">
              <div class="pipeline-dot"></div>
              <span class="pipeline-label">Lead Captured</span>
              <div class="pipeline-bar-wrap"><div class="pipeline-bar" style="width:100%"></div></div>
            </div>
            <div class="pipeline-row">
              <div class="pipeline-dot"></div>
              <span class="pipeline-label">Follow-Up Sent</span>
              <div class="pipeline-bar-wrap"><div class="pipeline-bar" style="width:82%"></div></div>
            </div>
            <div class="pipeline-row">
              <div class="pipeline-dot"></div>
              <span class="pipeline-label">Appointment Booked</span>
              <div class="pipeline-bar-wrap"><div class="pipeline-bar" style="width:64%"></div></div>
            </div>
            <div class="pipeline-row">
              <div class="pipeline-dot"></div>
              <span class="pipeline-label">Proposal Sent</span>
              <div class="pipeline-bar-wrap"><div class="pipeline-bar" style="width:45%"></div></div>
            </div>
            <div class="pipeline-row">
              <div class="pipeline-dot"></div>
              <span class="pipeline-label">Deal Closed</span>
              <div class="pipeline-bar-wrap"><div class="pipeline-bar" style="width:30%"></div></div>
            </div>
          </div>
          <div style="margin-top: 28px; padding-top: 24px; border-top: 1px solid var(--border);">
            <div class="section-label" style="margin-bottom: 12px;">Platforms I Automate With</div>
            <div class="tools-tags">
              <span class="tool-tag">GoHighLevel</span>
              <span class="tool-tag">Zapier</span>
              <span class="tool-tag">n8n</span>
              <span class="tool-tag">Make</span>
              <span class="tool-tag">Webhooks</span>
              <span class="tool-tag">API Integrations</span>
              <span class="tool-tag">AI / GPT</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- FUNNELS -->
<section id="funnels">
  <div class="container">
    <div class="section-label reveal">Portfolio</div>
    <h2 class="section-title reveal">Funnels I've Built</h2>
    <p class="section-sub reveal">Real funnel systems designed to capture leads, nurture prospects, and convert visitors into customers. Click any to view the live demo.</p>
    <div class="funnels-grid">
      <a href="https://protect.dperalta.uk/home-page-133365" target="_blank" class="funnel-card reveal">
        <div class="funnel-emoji">🛡️</div>
        <h3>Insurance Lead Generation Funnel</h3>
        <p>Designed to capture insurance inquiries and convert prospects into qualified leads.</p>
        <div class="funnel-link">View Live Demo →</div>
      </a>
      <a href="https://deehomes.dperalta.uk/home-page-769588" target="_blank" class="funnel-card reveal reveal-delay-1">
        <div class="funnel-emoji">🏡</div>
        <h3>Real Estate Property Inquiry Funnel</h3>
        <p>Optimized for property inquiries, capturing buyer interest and booking consultations.</p>
        <div class="funnel-link">View Live Demo →</div>
      </a>
      <a href="https://athena-aesthetics.dperalta.uk/landing-page-page" target="_blank" class="funnel-card reveal reveal-delay-2">
        <div class="funnel-emoji">💉</div>
        <h3>Medical Clinic Appointment Funnel</h3>
        <p>Built to attract patients and automate appointment scheduling for clinics.</p>
        <div class="funnel-link">View Live Demo →</div>
      </a>
      <a href="https://webraiders.dperalta.uk/home-660256" target="_blank" class="funnel-card reveal reveal-delay-3">
        <div class="funnel-emoji">💈</div>
        <h3>Salon & Barbershop Funnel</h3>
        <p>Promotional funnel designed to increase bookings for barbershops and salons.</p>
        <div class="funnel-link">View Live Demo →</div>
      </a>
      <a href="https://weoutdoor.dperalta.uk/lead-magnet-inbound-funnel-main-page-9189" target="_blank" class="funnel-card reveal reveal-delay-4">
        <div class="funnel-emoji">🛒</div>
        <h3>E-Commerce Lead Magnet Funnel</h3>
        <p>Lead generation funnel used to grow email lists and drive product sales.</p>
        <div class="funnel-link">View Live Demo →</div>
      </a>
      <a href="https://homepro.dperalta.uk/estimate" target="_blank" class="funnel-card reveal reveal-delay-5">
        <div class="funnel-emoji">🔨</div>
        <h3>Home Renovation Estimate Funnel</h3>
        <p>Built for contractors to generate renovation leads and estimate requests.</p>
        <div class="funnel-link">View Live Demo →</div>
      </a>
    </div>
  </div>
</section>

<!-- RESUME -->
<section id="resume">
  <div class="container">
    <div class="section-label reveal">Resume</div>
    <h2 class="section-title reveal">Background &<br />Experience</h2>
    <p class="section-sub reveal">A results-driven specialist with deep roots in sales, team leadership, and modern marketing automation.</p>
    <div class="resume-grid">
      <div>
        <div class="resume-skills">
          <h3>Core Skills</h3>
          <div class="skill-pills">
            <span class="skill-pill">GoHighLevel</span>
            <span class="skill-pill">CRM Pipelines</span>
            <span class="skill-pill">Funnel Design</span>
            <span class="skill-pill">Zapier</span>
            <span class="skill-pill">n8n</span>
            <span class="skill-pill">Make</span>
            <span class="skill-pill">SMS Automation</span>
            <span class="skill-pill">Email Sequences</span>
            <span class="skill-pill">API / Webhooks</span>
            <span class="skill-pill">Lead Generation</span>
            <span class="skill-pill">Booking Systems</span>
            <span class="skill-pill">AI Automation</span>
            <span class="skill-pill">Figma</span>
            <span class="skill-pill">Canva</span>
          </div>
          <h3>Education</h3>
          <div class="edu-card reveal">
            <h4>Bachelor of Business Administration</h4>
            <span>Management · Philippine School of Business Administration · 1995–1999</span>
          </div>
        </div>
      </div>
      <div class="resume-exp">
        <div class="exp-item reveal">
          <div class="exp-header">
            <div class="exp-title">GoHighLevel Funnel & Automation Specialist</div>
            <span class="exp-date">2024 – Present</span>
          </div>
          <div class="exp-company">Freelance / Self-Employed</div>
          <ul class="exp-bullets">
            <li>Design and deploy high-converting funnels across real estate, clinics, salons, and e-commerce.</li>
            <li>Build CRM pipelines, automated email/SMS workflows, and lead routing systems in GHL.</li>
            <li>Integrate platforms using Zapier, n8n, and webhooks for seamless cross-tool automation.</li>
          </ul>
        </div>
        <div class="exp-item reveal reveal-delay-1">
          <div class="exp-header">
            <div class="exp-title">Senior Financial Sales Associate</div>
            <span class="exp-date">Jul – Nov 2025</span>
          </div>
          <div class="exp-company">EastWest Ageas Insurance Company</div>
          <ul class="exp-bullets">
            <li>Delivered personalized financial solutions and closed insurance and investment products.</li>
            <li>Maintained high client engagement and retention through structured follow-ups.</li>
          </ul>
        </div>
        <div class="exp-item reveal reveal-delay-2">
          <div class="exp-header">
            <div class="exp-title">Senior Financial Sales Associate</div>
            <span class="exp-date">Feb – Jun 2025</span>
          </div>
          <div class="exp-company">Manulife-Chinabank Life Assurance Corporation</div>
          <ul class="exp-bullets">
            <li>Consulted clients on financial strategies and sold insurance/investment products.</li>
            <li>Managed pipelines and improved client conversion through structured engagement.</li>
          </ul>
        </div>
        <div class="exp-item reveal reveal-delay-3">
          <div class="exp-header">
            <div class="exp-title">Agency Manager</div>
            <span class="exp-date">Mar 2017 – Apr 2023</span>
          </div>
          <div class="exp-company">Care Health Systems International, Inc.</div>
          <ul class="exp-bullets">
            <li>Led sales and marketing initiatives to drive business growth and brand visibility.</li>
            <li>Developed recruitment and training systems to improve team productivity.</li>
          </ul>
        </div>
        <div class="exp-item reveal reveal-delay-4">
          <div class="exp-header">
            <div class="exp-title">Agency Manager – Alternative Marketing</div>
            <span class="exp-date">Mar 2006 – Feb 2016</span>
          </div>
          <div class="exp-company">Caritas Health Shield Inc.</div>
          <ul class="exp-bullets">
            <li>Built and scaled multi-level sales teams achieving consistent revenue targets.</li>
            <li>Implemented data-driven strategies to improve sales performance and efficiency.</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- TOOLS -->
<section id="tools">
  <div class="container">
    <div class="section-label reveal">Tech Stack</div>
    <h2 class="section-title reveal">Tools & Platforms<br />I Work With</h2>
    <p class="section-sub reveal">The technology stack I use to build funnels, landing pages, and automated marketing systems.</p>
    <div class="tools-grid">
      <div class="tool-card reveal">
        <div class="tool-em">🚀</div>
        <div>
          <h3>GoHighLevel</h3>
          <p>Funnels, CRM pipelines and marketing automation systems.</p>
        </div>
      </div>
      <div class="tool-card reveal reveal-delay-1">
        <div class="tool-em">⚡</div>
        <div>
          <h3>Zapier</h3>
          <p>Automation and integrations between marketing platforms.</p>
        </div>
      </div>
      <div class="tool-card reveal reveal-delay-2">
        <div class="tool-em">🔁</div>
        <div>
          <h3>n8n & Make</h3>
          <p>Advanced self-hosted and cloud workflow automation.</p>
        </div>
      </div>
      <div class="tool-card reveal reveal-delay-3">
        <div class="tool-em">🤖</div>
        <div>
          <h3>ChatGPT / Claude AI</h3>
          <p>AI-powered automation, workflow design and marketing content.</p>
        </div>
      </div>
      <div class="tool-card reveal reveal-delay-4">
        <div class="tool-em">🎨</div>
        <div>
          <h3>Figma</h3>
          <p>Designing landing pages, funnel layouts and responsive UI.</p>
        </div>
      </div>
      <div class="tool-card reveal reveal-delay-5">
        <div class="tool-em">🖌️</div>
        <div>
          <h3>Canva</h3>
          <p>Creating funnel visuals, marketing creatives and graphics.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section id="cta">
  <div class="cta-glow"></div>
  <div class="cta-inner">
    <div class="section-label reveal" style="text-align:center; display:block;">Get In Touch</div>
    <h2 class="reveal">Ready to Automate<br /><span>Your Business?</span></h2>
    <p class="reveal">Let's discuss how funnels, CRM automation, and workflow systems can help scale your business and increase conversions.</p>
    <a href="mailto:derrick@dperalta.uk" class="btn-primary reveal">Let's Connect</a>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">Derrick Peralta</div>
  <div>Funnel Builder & Automation Specialist · Philippines</div>
  <div>© 2025 · portfolio.dperalta.uk</div>
</footer>

<script>
  // NAV scroll effect
  const navbar = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    navbar.classList.toggle('scrolled', window.scrollY > 40);
  });

  // Scroll reveal
  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  revealEls.forEach(el => observer.observe(el));
</script>
</body>
</html>